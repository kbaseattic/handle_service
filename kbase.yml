# This file exists only to populate the version in the Impl file.y
module-name:
    AbstractHandle

module-description:
    Core KBase service that manages Handle objects.

service-language:
    perl

module-version:
    0.2.0

owners:
    [kkeller, scanon, mrcreosote]
